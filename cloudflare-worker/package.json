{
  "name": "sportmanager-api",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker API for Sport Manager",
  "scripts": {
    "login": "wrangler login",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "itty-router": "^1.0.0"
  },
  "devDependencies": {
    "wrangler": "^4.25.0"
  }
}
